if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }if (unit16.substr(0, 5) === "ff d8" && unit16.substr(unit16.length - 5, 5) === "ff d9") {
        const index = unit16.indexOf(" ff c0 ");
        const header = unit16.slice(0, index);
        console.log(header.indexOf(" ff e1 "));
    }